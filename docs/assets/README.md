# Assets

Drop the demo recording here as `demo.gif` (a short screen capture of bgterm
revealing on `⌥⌘T`, typing a command, and receding on `Esc`), then replace the
ASCII preview in the top-level [README](../../README.md) with:

```markdown
<p align="center"><img src="docs/assets/demo.gif" alt="bgterm demo" width="720"></p>
```

A still hero image (`hero.png`) can live here too.
